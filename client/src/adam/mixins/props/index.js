import spacingObj from './spacing'

export const spacing = spacingObj
// export const spacingProps = spacing

// export const appendSpacingProps = (arr, props) => {
//   const keys = Object.keys(spacing)
//   keys.forEach((k) => {
//     if (props[k]) {
//       arr.push(`${k}-${props[k]}`)
//     }
//   })

//   return arr
// }
