const state = {
  //
  splitView: {
    left: {
      bucket: '',
    },
  },
}

const getters = {
  splitView({ splitView }) {
    return splitView
  },
}

const mutations = {
  //
}

const actions = {
  //
}
export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions,
}
